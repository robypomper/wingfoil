/**
 * `workflow` module — Project Workflow pillar; phases/steps/atomic actions, `include()` composition
 * (P4.1). Scaffold stub: implementation lands in later v0.1 tasks.
 */
export const MODULE_NAME = 'workflow' as const;
