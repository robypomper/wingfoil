/**
 * `memory` module — Project Memory pillar; git-backed documents with per-type state machines
 * (P1.13). Scaffold stub: implementation lands in later v0.1 tasks.
 */
export const MODULE_NAME = 'memory' as const;
