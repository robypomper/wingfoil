/**
 * `storage` module — git-backed file storage (YAML + Markdown); single source of truth
 * (REQ-SYS-01). Scaffold stub: implementation lands in later v0.1 tasks.
 */
export const MODULE_NAME = 'storage' as const;
