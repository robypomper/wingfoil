/**
 * `core` module — shared domain logic; single behavior behind both the CLI and MCP surfaces
 * (REQ-SYS-05). Scaffold stub: implementation lands in later v0.1 tasks.
 */
export const MODULE_NAME = 'core' as const;
