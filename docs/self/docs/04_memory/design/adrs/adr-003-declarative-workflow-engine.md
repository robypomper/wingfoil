---
id: adr-003-declarative-workflow-engine
type: adr
title: ""              # REQUIRED — e.g. "Git-backed storage as single source of truth"
status: draft          # auto-set by wingfoil
sard_ref: ""           # REQUIRED — REQ-* code or feature ID, e.g. "REQ-SYS-01"
supersedes: ""         # optional — id of the ADR this one replaces, e.g. "adr-002"
tmpl_version: 260703   # Orignal template version
---

## Context

<!-- Why this decision was needed: the forces, constraints, and alternatives considered.
     Include what would happen without this decision. -->

## Decision

<!-- What was decided, stated clearly and directly. -->

## Consequences

<!-- What follows from this decision:
     - positive: what it enables
     - negative: trade-offs and constraints introduced
     - neutral: things that change but are neither good nor bad -->

## Process Notes

<!-- Optional running log, not the decision rationale (that belongs above): how the review
     actually went — alternatives raised during approval, reject/reopen rounds and why, tasks that
     later hit a gap this ADR didn't cover (candidate for a future ADR that supersedes this one). -->
